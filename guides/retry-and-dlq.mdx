---
title: "Retry & Dead Letter Queue"
description: "Handle failures with exponential backoff and DLQ"
icon: "rotate-right"
---

<Note>This page is a placeholder. Full content will be generated from the implemented code using `/generate-doc`.</Note>

## Overview

FlowWarden provides `@RetryPolicy` for exponential backoff retry and `@DeadLetterQueue` for storing events that exhaust all retries.
