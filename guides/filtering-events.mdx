---
title: "Filtering Events"
description: "Filter Change Stream events with @Filter"
icon: "filter"
---

<Note>This page is a placeholder. Full content will be generated from the implemented code using `/generate-doc`.</Note>

## Overview

The `@Filter` annotation lets you filter Change Stream events either server-side (MongoDB aggregation pipeline) or application-side.
